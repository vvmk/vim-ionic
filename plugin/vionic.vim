if !exists('g:vionic_use_dispatch')
  let g:vionic_use_dispatch = 0
endif

if !exists('g:vionic_stylesheet_format')
  let g:vionic_stylesheet_format = "scss"
endif

if !exists('g:vionic_create_spec')
  let g:vionic_create_spec = "off"
endif

if !exists('g:gnu_grep')
  let g:gnu_grep = "grep"
endif
